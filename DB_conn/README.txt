Write an Application that connects to a database server (e.g. Oracle,
DB2, MySQL, SQL Server…etc) using a vendor-specific JDBC
Driver (Type 4).
• Execute different types of queries that manipulate the data in the
tables (retrieve, insert, edit, delete).
• Try the Commit and Rollback features